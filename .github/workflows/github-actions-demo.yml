name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install PHP dependencies
        run: composer install --prefer-dist --no-scripts --no-progress --no-suggest

      - name: Install Node.js dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
            server: sc527094.ftp.tools
            username: ${{ secrets.FTP_USERNAME }} # Убедитесь, что этот секрет правильно установлен в настройках репозитория
            password: ${{ secrets.FTP_PASSWORD }} # То же самое для этого секрета
            port: 21 # Порт по умолчанию для FTP, вы можете опустить эту строку, так как 21 - это стандартный порт
            protocol: sftp # Используйте протокол FTP
            local-dir: ./ # Каталог, из которого будут загружаться файлы
            dangerous-clean-slate: false # Не удалять файлы на сервере, которых нет в репозитории
