name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.PAT }} # Используйте PAT для аутентификации

      - name: Install PHP dependencies
        run: composer install --prefer-dist --no-scripts --no-progress --no-suggest

      - name: Install Node.js dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Configure Git
        run: |
          git config user.name Maaaaxim
          git config user.email maxim.kubichka@gmail.com

      - name: Commit and Push
        run: |
          git add -f public/build/
          git commit -m "Build for production"
          git push origin HEAD:main # Предполагая, что вы хотите пушить в ветку main
